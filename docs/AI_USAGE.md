# AI Usage Declaration

Declaring AI use does not reduce the grade. You must be able to explain and validate every submitted decision.

| Tool      | Activity          | Prompt / purpose                                               | How I validated the result                                                  | What I changed / rejected                                         |
|-----------|-------------------|----------------------------------------------------------------|-----------------------------------------------------------------------------|-------------------------------------------------------------------|
| ChatGPT | Documentation and testing | Support for the ADR, API contract, architecture diagrams and a few CONNECTOR tests. | Compared the documentation with the implemented code, ran the test suite and manually validated the main Board flow. | Adapted the documentation to the final project and reviewed the suggested tests before including them. |
| Claude    | Code and analysis | Support with code and understanding the exercise requirements. | We reviewed the code and compared it with the requirements of each section. | We adapted the suggestions according to the project requirements. |
| Claude    | Frontend (my part: board-view.js) | Lo usé como guía para armar mi parte (board-view.js): renderizar RECTANGLE/TEXT/CONNECTOR en SVG. | Levanté el backend local y probé todo a mano en el navegador: cree un board, agregué rectángulo y texto, los moví, los conecté, los borré y guardé/recargué para confirmar que quedaba. | Le pedí que le quitara los prompt()/alert() del navegador y los cambiara por inputs normales del toolbar, y le cambié los colores a mi gusto. |
| Amazon Q  | API Client (my part: board-api-client.js) | Lo usé para guiarme en la implementación de board-api-client.js: aislar todo el acceso HTTP, manejar errores con ApiClientError e integrar Loading/Success/Error/Retry en app.js. | Ejecuté mvn test (12 tests, 0 fallos) y verifiqué manualmente que POST/GET/PUT funcionan contra el backend, que los estados Loading/Success/Error son visibles en la UI y que Retry reintenta la última operación fallida. | Ajusté la documentación del módulo para que describiera con precisión las funciones expuestas y el contrato de errores. |
| Amazon Q  | Lab 6 — ELEMENT_MOVED + ELEMENT_CREATED + UI | Lo usé para implementar mi parte del Lab 6: `applyElementMoved` en `BoardEventApplicationService`, publicar y recibir `ELEMENT_MOVED` en el frontend, conectar STOMP al crear/cargar un board, implementar `applyElementCreated` que faltaba, y mejorar el diseño visual del tablero. | Levanté la app localmente, abrí dos navegadores con el mismo boardId y verifiqué que mover un elemento en uno se refleja en el otro, que crear elementos se propaga, y que un board distinto no recibe nada. También corrí los tests existentes para confirmar que nada se rompió. | Alineé el contrato de eventos con el módulo de Mabel (`board-realtime-client.js`) después de ver que usaba un payload distinto al que yo había implementado inicialmente. Ajusté el diseño visual según el feedback del equipo. |